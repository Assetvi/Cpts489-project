<!DOCTYPE html>
<html lang="en">
<!-- Links and site icon and name content -->

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
</head>

<body>
    <%- include("header") %>
        <h2>Users</h2>
        <ul>
            <% for ( user of users){%>
                <li><a href = <%= "/profile/"+user.username %>><%= user.username%></a>
                    <% if (friends.includes(user.username)){ %>
                        <p>A friend!</p>
                    <% }else{ %>
                    <button>Add Friend</button>
                    <%}%>
                </li>
                    <%}%>
        </ul>
</body>