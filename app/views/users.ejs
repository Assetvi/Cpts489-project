<!DOCTYPE html>
<html lang="en">
    <!-- Links and site icon and name content -->

    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="css/main.css" />
        <link rel="icon" href="images/Icon.png"> <!-- Icon of page title -->
        <link href="https://fonts.googleapis.com/css?family=Roboto&display=swap"
            rel="stylesheet">
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
            rel="stylesheet">
        <script src="../controllers/friendsController.js"></script>
        <title>Movie Ratings | Friends</title>
    </head>

    <body>
        <%- include("header") %>
        <hr style="border-top: 5px solid; margin: 0px 0px 3rem">
        <div class="container" style="margin: 100px 10rem; width: 77%;">
            <div class="row">
                <div class="col">
                    
                    <h2>Users</h2>
                    <ul>
                        <% for ( user of users){%>
                        <li><a style="margin-bottom: 50px; color:#408558;" href
                                = <%="/profile/" +user.username %>><%=
                                user.username%></a>
                            <% if (friends.includes(user.username)){ %>
                            <a class="pagebutton" href =
                                <%="/users/removefriend/"
                                +user.username%>>Remove Friend</a>
                            <% }else{ %>
                            <a class="pagebutton" href = <%="/users/addfriend/"
                                +user.username%>>Add Friend</a>
                            <%}%>
                        </li>
                        <br>
                        <%}%>
                    </ul>
                    <a class="sitebutton" href="friends">Back to Friends</a>
                </div>
            </div>
        </div>
    </body>