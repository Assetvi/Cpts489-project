<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="css/main.css" />
        <link rel="icon" href="images/Icon.png"> <!-- Icon of page title -->
        <link href="https://fonts.googleapis.com/css?family=Roboto&display=swap"
            rel="stylesheet">
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
            rel="stylesheet">
        <script src="../controllers/usermovieController.js"></script>
        <title> Movie Ratings | Movie Details </title>
    </head>
    <body>
        <!-- Navbar content -->
        <header>
            <%- include("header") %>
        </header>

        <!-- Page content -->
        <hr style="border-top: 5px solid; margin: 0px 0px 3rem">
        <div class="container" style="margin: 100px 10rem; width: 77%;">
            <div class="row">
                <div class="col">
                   
                    <h1
                        style="margin-bottom: 2rem; color:#222529; font-weight: bold;">
                        <%= movie.title %> </h1>

                    <!-- Watch later and already watched buttons -->
                    <div>
                        <a href="/" class="sitebutton">
                            <span class="sitebuttonContent">Add to watch
                                later</span>
                        </a>

                        <a href="/" class="sitebutton">
                            <span class="sitebuttonContent">Add to already
                                watched</span>
                        </a>
                    </div>

                    <!-- Movie details -->
                    <div id="movieDetails">
                        
                        <p class="subTitle">Release Date: </p>
                        <p class="subTitle"
                            style="color: #408558; margin-bottom: 30px;"><%=
                            movie.year %></p>
                        <!-- Display movie genres -->
                        <p class="subTitle">Genres:</p>
                        <% if (movie.genre && movie.genre.length > 0) { %>
                        
                        <span class="subTitle"
                            style="color: #408558; margin-right: 15px;"><%=
                            movie.genre %></span>
                        
                        <% } else { %>
                        <p>No genre found</p>
                        <% } %>
                        <!-- Display movie overview -->
                        <p class="subTitle" style="margin-top: 30px">Director:
                        </p>
                        <p class="subTitle"
                            style="font-weight: normal; margin-bottom: 30px; width: 65%;"><%=
                            movie.director %></p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Page end -->
        <div id="pageEnd"></div>
    </body>
</html>
